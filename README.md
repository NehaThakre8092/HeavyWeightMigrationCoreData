Used only when:

You change attribute type (String → Int)

You split/merge entities

You add derived fields

Need to run custom transformation logic

This is rare because:

It needs .xcmappingmodel

It needs NSEntityMigrationPolicy

It is time-consuming
